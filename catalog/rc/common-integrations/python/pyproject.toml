[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "runtimeconditions-common-integrations"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["runtimeconditions_common_integrations"]

